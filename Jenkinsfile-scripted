pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo "Build stage"
        script {
            def example = load "example.groovy"
            example1 = example.exampleMethod()
        }
      }
    }
    stage("deploy") {
      steps {
        echo "Deploy stage"
      }
    }
  }
}
