node {
    
    stage('Build') {
        
       echo "This is build stage"
    }
    stage("test") {
      when {
        expression {
          BRANCH_NAME=dev
        }
      }
      steps {
        echo "Test stage"
      }
    }
}
