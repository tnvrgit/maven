pipeline {
  agent any
  def BRANCH_NAME=def
  stages {
    stage("build") {
      steps {
        echo "Build stage"
      }
    }
    stage("deploy") {
      steps {
        echo "Deploy stage"
      }
    }
  }
}
